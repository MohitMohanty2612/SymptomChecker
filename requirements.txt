# SymptomChat — Python dependencies
# Install all:  pip install -r requirements.txt

python>=3.10.13
flask
scikit-learn
tensorflow>=2.15          # includes Keras; for Apple Silicon use tensorflow-macos
spacy
nltk
numpy
scipy
colorama

# Optional — production deployment
gunicorn
redis              # for multi-instance session store
flask-limiter      # rate limiting
